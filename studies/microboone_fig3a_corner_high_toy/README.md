# Fig. 3a right-lower corner high-Toy study

Evaluate every production-grid point in
`0.7 <= sin^2(2 theta_mue) <= 1` and
`0.01 <= Delta m^2_41 <= 0.05 eV^2`.

Every Toy is profiled separately. Raw Toy test statistics are checkpointed in
CSV. Nested prefixes of the same deterministic Toy streams are used to compare
500, 1000, 2000 and 5000-Toy contours without mixing independent runs.

